from __future__ import annotations

import pandas as pd


def train_long_term_models(long_term_training: pd.DataFrame) -> object:
    # Placeholder for long-term model training implementation.
    """Train long-term forecasting models. Implementation is intentionally pending."""
    raise NotImplementedError("Long-term model training will be implemented after final model design is selected.")

